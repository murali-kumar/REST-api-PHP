REST API FORMAT

// return all records
GET /users

// return a specific record
GET /users/{id}

// create a new record
POST /users

// update an existing record
PUT /users/{id}

// delete an existing record
DELETE /users/{id}
